---
name: Create ModelChangeWarningDialog component
status: completed
created: 2026-01-23T08:40:46Z
updated: 2026-01-23T09:45:00Z
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Create ModelChangeWarningDialog component

## Description
Create a new AlertDialog component that warns users when switching between executor variants with different models (e.g., Sonnet → Opus). This dialog will inform users that switching models starts a fresh session without prior context.

## Acceptance Criteria
- [x] Component file created at `frontend/src/components/dialogs/ModelChangeWarningDialog.tsx`
- [x] Uses shadcn/ui AlertDialog components
- [x] Shows previous and new variant names with their models
- [x] Has "Cancel" and "Continue Anyway" actions
- [x] Props typed correctly with TypeScript

## Technical Details

**File Location**: `frontend/src/components/dialogs/ModelChangeWarningDialog.tsx`

**Props Interface**:
```typescript
type Props = {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  previousVariant: string;
  previousModel: string | null;
  newVariant: string;
  newModel: string | null;
  onConfirm: () => void;
};
```

**Component Structure**:
- AlertDialog wrapper with controlled `open` state
- AlertDialogTitle: "Model Change Detected"
- AlertDialogDescription: Shows variant change details and warning about fresh session
- AlertDialogFooter: Cancel and "Continue Anyway" buttons
- onConfirm callback triggers when user confirms

**Import Requirements**:
- All AlertDialog components from `@/components/ui/alert-dialog`

## Dependencies
- shadcn/ui alert-dialog component must be available

## Effort Estimate
- Size: XS
- Hours: 0.5
- Parallel: true

## Definition of Done
- [x] Component file created with correct imports
- [x] TypeScript types defined correctly
- [x] Dialog renders with proper styling
- [x] Cancel action closes dialog
- [x] Confirm action calls onConfirm callback
- [x] No TypeScript errors
- [x] Follows project code style conventions
