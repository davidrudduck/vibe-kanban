---
name: Add retry button UI in TemplatePicker error state
status: open
created: 2026-01-11T10:25:38Z
updated: 2026-01-11T10:25:38Z
depends_on: [007]
conflicts_with: [006]
---

# Task: Add retry button UI in TemplatePicker error state

## Description
Implement the retry button UI that appears when there's an error and the onRetry callback is provided.

## Acceptance Criteria
- [ ] Retry button appears when error prop is set and onRetry is provided
- [ ] Retry button hidden when no error
- [ ] Button uses i18n key `templatePicker.retry`
- [ ] Button has proper styling (outline variant, small size)

## Technical Details
- File: `frontend/src/components/tasks/TemplatePicker.tsx`
- Replace error section (~line 293-298) with:

```tsx
{error && !loading && (
  <div className="flex flex-col items-center py-8 gap-3">
    <div className="text-center text-destructive">{error}</div>
    {onRetry && (
      <Button variant="outline" size="sm" onClick={onRetry}>
        {t('templatePicker.retry', 'Retry')}
      </Button>
    )}
  </div>
)}
```

## Dependencies
- [ ] onRetry prop must be added (task 007)

## Effort Estimate
- Size: S
- Hours: 0.5

## Definition of Done
- [ ] Retry button UI implemented
- [ ] Tests pass (TDD green phase)
